package sec1;

public class Example1_배열 {

    public static void main(String[] args) {

        //배열 선언 -> 메모리 할당x
        int[] a;
        int b[];
        //System.out.println("a[0] = " + a[0]); //메모리 할당안된 상태이기 때문에 에러

        //메모리 할당 됨 -> 선언과 할당
        int[] c = new int[3];
        System.out.println("c[0] = " + c[0]); // 0

        //d는 primitive 타입이므로 반드시 초기화 필요함
        int d;
        //System.out.println("d" + d); //에러 -> 초기화 안해서











    }
}
